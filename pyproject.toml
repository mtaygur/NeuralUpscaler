[project]
name = "neuralupscaler"
version = "0.1.0"
description = "Neural network-based video upscaling with HDR support"
requires-python = ">=3.14"
dependencies = [
    "ffmpeg-python>=0.2.0",
    "numpy>=1.24.0",
]
