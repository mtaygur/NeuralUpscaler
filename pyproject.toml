[project]
name = "neuralupscaler"
version = "0.1.0"
description = "Neural network-based video upscaling with HDR support"
requires-python = ">=3.11"
dependencies = [
    "ffmpeg>=1.4",
    "ffmpeg-python>=0.2.0",
    "numpy>=1.24.0",
    "opencv-python>=4.8.0",
    "scikit-image>=0.21.0",
]
